import { Course } from '../../types';

export const sqlCourse: Course = {
  id: 'sql',
  title: 'SQL Relacional Master',
  subtitle: 'Consultas, Joins, Subqueries, Vistas y Transacciones',
  icon: 'Database',
  color: 'from-blue-500 to-indigo-600',
  badgeBg: 'bg-indigo-500/10 text-indigo-400 border-indigo-500/20',
  description: 'Aprende bases de datos relacionales con PostgreSQL/SQLite: SELECT, JOINs, agregaciones, subconsultas complejas, índices, CTEs y transacciones ACID.',
  lessons: [
    // --- NIVEL BÁSICO (1-10) ---
    {
      id: 'sql-1',
      number: 1,
      title: 'Consultas Básicas con SELECT',
      level: 'Básico',
      summary: 'Recuperación de columnas de una tabla relacional.',
      theoryMarkdown: `### Introducción a SQL\n\n\`\`\`sql\nSELECT nombre, precio FROM productos;\n\`\`\``,
      instructions: 'Escribe una consulta para seleccionar todas las columnas (`*`) de la tabla `usuarios`.',
      initialCode: `-- Escribe tu consulta SQL aquí\n`,
      solutionCode: `SELECT * FROM usuarios;`,
      testCases: [{ id: 'tc1', description: 'Selecciona todas las columnas', expectedOutput: 'SELECT * FROM usuarios;' }],
      hint: 'SELECT * FROM usuarios;'
    },
    {
      id: 'sql-2',
      number: 2,
      title: 'Filtrado de Datos con WHERE',
      level: 'Básico',
      summary: 'Filtra registros que cumplan condiciones especificadas.',
      theoryMarkdown: `### Cláusula WHERE\n\n\`\`\`sql\nSELECT * FROM productos WHERE precio > 50;\n\`\`\``,
      instructions: 'Filtra la tabla `clientes` donde `pais = "Mexico"`.',
      initialCode: `-- Filtrar clientes de Mexico\n`,
      solutionCode: `SELECT * FROM clientes WHERE pais = 'Mexico';`,
      testCases: [{ id: 'tc2', description: 'Filtra clientes de Mexico', expectedOutput: "SELECT * FROM clientes WHERE pais = 'Mexico';" }],
      hint: "SELECT * FROM clientes WHERE pais = 'Mexico';"
    },
    {
      id: 'sql-3',
      number: 3,
      title: 'Operadores LIKE, IN y BETWEEN',
      level: 'Básico',
      summary: 'Coincidencia de patrones (% y _) y rangos de datos.',
      theoryMarkdown: `### Operador LIKE\n\n\`\`\`sql\nSELECT * FROM usuarios WHERE nombre LIKE 'A%';\n\`\`\``,
      instructions: 'Selecciona los productos con `precio BETWEEN 10 AND 50`.',
      initialCode: `-- Productos con precio entre 10 y 50\n`,
      solutionCode: `SELECT * FROM productos WHERE precio BETWEEN 10 AND 50;`,
      testCases: [{ id: 'tc3', description: 'Filtra por rango de precio', expectedOutput: 'SELECT * FROM productos WHERE precio BETWEEN 10 AND 50;' }],
      hint: 'SELECT * FROM productos WHERE precio BETWEEN 10 AND 50;'
    },
    {
      id: 'sql-4',
      number: 4,
      title: 'Ordenamiento con ORDER BY',
      level: 'Básico',
      summary: 'Orden ascendente (ASC) o descendente (DESC).',
      theoryMarkdown: `### ORDER BY\n\n\`\`\`sql\nSELECT * FROM empleados ORDER BY salario DESC;\n\`\`\``,
      instructions: 'Selecciona todos los productos ordenados por `precio` de forma descendente (`DESC`).',
      initialCode: `-- Ordenar por precio descendente\n`,
      solutionCode: `SELECT * FROM productos ORDER BY precio DESC;`,
      testCases: [{ id: 'tc4', description: 'Ordena descendente', expectedOutput: 'SELECT * FROM productos ORDER BY precio DESC;' }],
      hint: 'ORDER BY precio DESC;'
    },
    {
      id: 'sql-5',
      number: 5,
      title: 'Restricción de Resultados con LIMIT',
      level: 'Básico',
      summary: 'Paginación básica limitando la cantidad de filas devueltas.',
      theoryMarkdown: `### LIMIT\n\n\`\`\`sql\nSELECT * FROM articulos LIMIT 5;\n\`\`\``,
      instructions: 'Obtén los primeros 3 productos de la tabla `productos`.',
      initialCode: `-- Limitar a 3 productos\n`,
      solutionCode: `SELECT * FROM productos LIMIT 3;`,
      testCases: [{ id: 'tc5', description: 'Limita a 3 resultados', expectedOutput: 'SELECT * FROM productos LIMIT 3;' }],
      hint: 'LIMIT 3;'
    },
    {
      id: 'sql-6',
      number: 6,
      title: 'Funciones de Agregación: COUNT',
      level: 'Básico',
      summary: 'Conteo total de registros en una tabla o grupo.',
      theoryMarkdown: `### COUNT\n\n\`\`\`sql\nSELECT COUNT(*) FROM usuarios;\n\`\`\``,
      instructions: 'Cuenta el total de filas en la tabla `pedidos`.',
      initialCode: `-- Contar pedidos\n`,
      solutionCode: `SELECT COUNT(*) FROM pedidos;`,
      testCases: [{ id: 'tc6', description: 'Cuenta registros', expectedOutput: 'SELECT COUNT(*) FROM pedidos;' }],
      hint: 'SELECT COUNT(*) FROM pedidos;'
    },
    {
      id: 'sql-7',
      number: 7,
      title: 'Funciones de Agregación: SUM y AVG',
      level: 'Básico',
      summary: 'Cálculo de sumas acumuladas y promedios aritméticos.',
      theoryMarkdown: `### SUM y AVG\n\n\`\`\`sql\nSELECT AVG(precio) FROM productos;\n\`\`\``,
      instructions: 'Calcula la suma total de la columna `monto` en la tabla `ventas`.',
      initialCode: `-- Suma total de monto\n`,
      solutionCode: `SELECT SUM(monto) FROM ventas;`,
      testCases: [{ id: 'tc7', description: 'Calcula la suma', expectedOutput: 'SELECT SUM(monto) FROM ventas;' }],
      hint: 'SELECT SUM(monto) FROM ventas;'
    },
    {
      id: 'sql-8',
      number: 8,
      title: 'Valores Mínimos y Máximos: MIN y MAX',
      level: 'Básico',
      summary: 'Obtención del valor límite inferior y superior.',
      theoryMarkdown: `### MIN / MAX\n\n\`\`\`sql\nSELECT MAX(precio) FROM productos;\n\`\`\``,
      instructions: 'Selecciona el precio máximo de la tabla `productos`.',
      initialCode: `-- Precio máximo\n`,
      solutionCode: `SELECT MAX(precio) FROM productos;`,
      testCases: [{ id: 'tc8', description: 'Obtiene el precio máximo', expectedOutput: 'SELECT MAX(precio) FROM productos;' }],
      hint: 'SELECT MAX(precio) FROM productos;'
    },
    {
      id: 'sql-9',
      number: 9,
      title: 'Agrupamiento con GROUP BY',
      level: 'Básico',
      summary: 'Agrupa filas con valores idénticos en filas de resumen.',
      theoryMarkdown: `### GROUP BY\n\n\`\`\`sql\nSELECT categoria, COUNT(*) FROM productos GROUP BY categoria;\n\`\`\``,
      instructions: 'Agrupa por `categoria` y cuenta cuántos productos hay en cada una.',
      initialCode: `-- Conteo por categoria\n`,
      solutionCode: `SELECT categoria, COUNT(*) FROM productos GROUP BY categoria;`,
      testCases: [{ id: 'tc9', description: 'Agrupa por categoría', expectedOutput: 'SELECT categoria, COUNT(*) FROM productos GROUP BY categoria;' }],
      hint: 'GROUP BY categoria;'
    },
    {
      id: 'sql-10',
      number: 10,
      title: 'Filtrado de Grupos con HAVING',
      level: 'Básico',
      summary: 'Filtrado condicional sobre agregaciones (se aplica tras el GROUP BY).',
      theoryMarkdown: `### HAVING\n\n\`\`\`sql\nSELECT categoria, COUNT(*) FROM productos GROUP BY categoria HAVING COUNT(*) > 5;\n\`\`\``,
      instructions: 'Agrupa por `categoria` y muestra solo aquellas categorías que tengan más de 3 productos (`HAVING COUNT(*) > 3`).',
      initialCode: `-- GROUP BY y HAVING > 3\n`,
      solutionCode: `SELECT categoria, COUNT(*) FROM productos GROUP BY categoria HAVING COUNT(*) > 3;`,
      testCases: [{ id: 'tc10', description: 'Filtra grupos con HAVING', expectedOutput: 'SELECT categoria, COUNT(*) FROM productos GROUP BY categoria HAVING COUNT(*) > 3;' }],
      hint: 'HAVING COUNT(*) > 3;'
    },

    // --- NIVEL INTERMEDIO (11-20) ---
    {
      id: 'sql-11',
      number: 11,
      title: 'Combinación de Tablas: INNER JOIN',
      level: 'Intermedio',
      summary: 'Unión de tablas mediante claves primarias y foráneas relacionadas.',
      theoryMarkdown: `### INNER JOIN\n\n\`\`\`sql\nSELECT e.nombre, d.departamento \nFROM empleados e \nINNER JOIN departamentos d ON e.depto_id = d.id;\n\`\`\``,
      instructions: 'Realiza un `INNER JOIN` entre `pedidos p` y `clientes c` usando `ON p.cliente_id = c.id`.',
      initialCode: `-- INNER JOIN pedidos y clientes\n`,
      solutionCode: `SELECT * FROM pedidos p INNER JOIN clientes c ON p.cliente_id = c.id;`,
      testCases: [{ id: 'tc11', description: 'Realiza INNER JOIN', expectedOutput: 'SELECT * FROM pedidos p INNER JOIN clientes c ON p.cliente_id = c.id;' }],
      hint: 'INNER JOIN clientes c ON p.cliente_id = c.id;'
    },
    {
      id: 'sql-12',
      number: 12,
      title: 'Combinación Externa: LEFT JOIN',
      level: 'Intermedio',
      summary: 'Mantiene todas las filas de la tabla izquierda aunque no haya coincidencia.',
      theoryMarkdown: `### LEFT JOIN\n\n\`\`\`sql\nSELECT c.nombre, p.id \nFROM clientes c \nLEFT JOIN pedidos p ON c.id = p.cliente_id;\n\`\`\``,
      instructions: 'Selecciona los clientes con `LEFT JOIN` a la tabla `pedidos`.',
      initialCode: `-- LEFT JOIN clientes con pedidos\n`,
      solutionCode: `SELECT * FROM clientes c LEFT JOIN pedidos p ON c.id = p.cliente_id;`,
      testCases: [{ id: 'tc12', description: 'Realiza LEFT JOIN', expectedOutput: 'SELECT * FROM clientes c LEFT JOIN pedidos p ON c.id = p.cliente_id;' }],
      hint: 'LEFT JOIN pedidos p ON c.id = p.cliente_id;'
    },
    {
      id: 'sql-13',
      number: 13,
      title: 'Subconsultas en la Cláusula WHERE',
      level: 'Intermedio',
      summary: 'Consultas anidadas para filtrar dinámicamente.',
      theoryMarkdown: `### Subqueries\n\n\`\`\`sql\nSELECT * FROM productos WHERE precio > (SELECT AVG(precio) FROM productos);\n\`\`\``,
      instructions: 'Selecciona los productos cuyo precio sea mayor al promedio general `(SELECT AVG(precio) FROM productos)`.',
      initialCode: `-- Subconsulta de precio promedio\n`,
      solutionCode: `SELECT * FROM productos WHERE precio > (SELECT AVG(precio) FROM productos);`,
      testCases: [{ id: 'tc13', description: 'Subconsulta con promedio', expectedOutput: 'SELECT * FROM productos WHERE precio > (SELECT AVG(precio) FROM productos);' }],
      hint: 'WHERE precio > (SELECT AVG(precio) FROM productos);'
    },
    {
      id: 'sql-14',
      number: 14,
      title: 'Inserción de Datos con INSERT INTO',
      level: 'Intermedio',
      summary: 'Agregación de nuevos registros a una tabla relacional.',
      theoryMarkdown: `### INSERT INTO\n\n\`\`\`sql\nINSERT INTO usuarios (nombre, email) VALUES ('Ana', 'ana@mail.com');\n\`\`\``,
      instructions: 'Inserta en `usuarios (nombre, email)` los valores `("Carlos", "carlos@test.com")`.',
      initialCode: `-- INSERT INTO usuarios\n`,
      solutionCode: `INSERT INTO usuarios (nombre, email) VALUES ('Carlos', 'carlos@test.com');`,
      testCases: [{ id: 'tc14', description: 'Inserta usuario', expectedOutput: "INSERT INTO usuarios (nombre, email) VALUES ('Carlos', 'carlos@test.com');" }],
      hint: "INSERT INTO usuarios (nombre, email) VALUES ('Carlos', 'carlos@test.com');"
    },
    {
      id: 'sql-15',
      number: 15,
      title: 'Actualización de Datos con UPDATE',
      level: 'Intermedio',
      summary: 'Modificación de columnas existentes filtradas por WHERE.',
      theoryMarkdown: `### UPDATE\n\n\`\`\`sql\nUPDATE usuarios SET activo = true WHERE id = 10;\n\`\`\``,
      instructions: 'Actualiza el `precio = 100` en la tabla `productos` para `id = 5`.',
      initialCode: `-- UPDATE productos\n`,
      solutionCode: `UPDATE productos SET precio = 100 WHERE id = 5;`,
      testCases: [{ id: 'tc15', description: 'Actualiza precio por ID', expectedOutput: 'UPDATE productos SET precio = 100 WHERE id = 5;' }],
      hint: 'UPDATE productos SET precio = 100 WHERE id = 5;'
    },
    {
      id: 'sql-16',
      number: 16,
      title: 'Eliminación de Datos con DELETE',
      level: 'Intermedio',
      summary: 'Borrado seguro de registros filtrados.',
      theoryMarkdown: `### DELETE FROM\n\n\`\`\`sql\nDELETE FROM usuarios WHERE activo = false;\n\`\`\``,
      instructions: 'Elimina de `pedidos` aquellos con `estado = "cancelado"`.',
      initialCode: `-- DELETE pedidos cancelados\n`,
      solutionCode: `DELETE FROM pedidos WHERE estado = 'cancelado';`,
      testCases: [{ id: 'tc16', description: 'Elimina pedidos cancelados', expectedOutput: "DELETE FROM pedidos WHERE estado = 'cancelado';" }],
      hint: "DELETE FROM pedidos WHERE estado = 'cancelado';"
    },
    {
      id: 'sql-17',
      number: 17,
      title: 'Creación de Tablas con CREATE TABLE',
      level: 'Intermedio',
      summary: 'Definición de esquemas, tipos y claves primarias.',
      theoryMarkdown: `### DDL: CREATE TABLE\n\n\`\`\`sql\nCREATE TABLE cursos (\n  id INT PRIMARY KEY,\n  titulo VARCHAR(100)\n);\n\`\`\``,
      instructions: 'Crea la tabla `roles` con columna `id INT PRIMARY KEY` y `nombre VARCHAR(50)`.',
      initialCode: `-- CREATE TABLE roles\n`,
      solutionCode: `CREATE TABLE roles ( id INT PRIMARY KEY, nombre VARCHAR(50) );`,
      testCases: [{ id: 'tc17', description: 'Crea la tabla roles', expectedOutput: 'CREATE TABLE roles ( id INT PRIMARY KEY, nombre VARCHAR(50) );' }],
      hint: 'CREATE TABLE roles ( id INT PRIMARY KEY, nombre VARCHAR(50) );'
    },
    {
      id: 'sql-18',
      number: 18,
      title: 'Restricción FOREIGN KEY',
      level: 'Intermedio',
      summary: 'Mantenimiento de integridad referencial entre tablas.',
      theoryMarkdown: `### FOREIGN KEY\n\nGarantiza que los IDs referenciados existan en la tabla padre.`,
      instructions: 'Escribe la instrucción SQL para añadir una FK simple o simúlala.',
      initialCode: `-- Simular FK\n`,
      solutionCode: `ALTER TABLE pedidos ADD CONSTRAINT fk_cliente FOREIGN KEY (cliente_id) REFERENCES clientes(id);`,
      testCases: [{ id: 'tc18', description: 'Agrega Foreign Key', expectedOutput: 'ALTER TABLE pedidos ADD CONSTRAINT fk_cliente FOREIGN KEY (cliente_id) REFERENCES clientes(id);' }],
      hint: 'FOREIGN KEY (cliente_id) REFERENCES clientes(id)'
    },
    {
      id: 'sql-19',
      number: 19,
      title: 'Subconsultas Correlacionadas con EXISTS',
      level: 'Intermedio',
      summary: 'Evaluación de existencia por cada fila procesada.',
      theoryMarkdown: `### EXISTS\n\n\`\`\`sql\nSELECT * FROM clientes c \nWHERE EXISTS (SELECT 1 FROM pedidos p WHERE p.cliente_id = c.id);\n\`\`\``,
      instructions: 'Selecciona clientes que tengan al menos un pedido usando `EXISTS`.',
      initialCode: `-- SELECT con EXISTS\n`,
      solutionCode: `SELECT * FROM clientes c WHERE EXISTS (SELECT 1 FROM pedidos p WHERE p.cliente_id = c.id);`,
      testCases: [{ id: 'tc19', description: 'Consulta con EXISTS', expectedOutput: 'SELECT * FROM clientes c WHERE EXISTS (SELECT 1 FROM pedidos p WHERE p.cliente_id = c.id);' }],
      hint: 'WHERE EXISTS (SELECT 1 FROM pedidos p WHERE p.cliente_id = c.id);'
    },
    {
      id: 'sql-20',
      number: 20,
      title: 'Operaciones UNION y UNION ALL',
      level: 'Intermedio',
      summary: 'Combinación vertical de conjuntos de datos con la misma estructura.',
      theoryMarkdown: `### UNION vs UNION ALL\n\nUNION elimina duplicados; UNION ALL los conserva.`,
      instructions: 'Combina los nombres de `clientes` y `empleados` con `UNION`.',
      initialCode: `-- UNION de nombres\n`,
      solutionCode: `SELECT nombre FROM clientes UNION SELECT nombre FROM empleados;`,
      testCases: [{ id: 'tc20', description: 'Realiza UNION de nombres', expectedOutput: 'SELECT nombre FROM clientes UNION SELECT nombre FROM empleados;' }],
      hint: 'SELECT nombre FROM clientes UNION SELECT nombre FROM empleados;'
    },

    // --- NIVEL AVANZADO (21-30) ---
    {
      id: 'sql-21',
      number: 21,
      title: 'Vistas en SQL (CREATE VIEW)',
      level: 'Avanzado',
      summary: 'Tablas virtuales persistentes basadas en consultas guardadas.',
      theoryMarkdown: `### CREATE VIEW\n\n\`\`\`sql\nCREATE VIEW vista_activos AS \nSELECT * FROM usuarios WHERE activo = true;\n\`\`\``,
      instructions: 'Crea una vista `vista_vip` con clientes con `nivel = "VIP"`.',
      initialCode: `-- CREATE VIEW vista_vip\n`,
      solutionCode: `CREATE VIEW vista_vip AS SELECT * FROM clientes WHERE nivel = 'VIP';`,
      testCases: [{ id: 'tc21', description: 'Crea la vista vista_vip', expectedOutput: "CREATE VIEW vista_vip AS SELECT * FROM clientes WHERE nivel = 'VIP';" }],
      hint: "CREATE VIEW vista_vip AS SELECT * FROM clientes WHERE nivel = 'VIP';"
    },
    {
      id: 'sql-22',
      number: 22,
      title: 'Índices y Optimización (CREATE INDEX)',
      level: 'Avanzado',
      summary: 'Estructuras B-Tree para acelerar búsquedas de lectura.',
      theoryMarkdown: `### CREATE INDEX\n\n\`\`\`sql\nCREATE INDEX idx_email ON usuarios(email);\n\`\`\``,
      instructions: 'Crea un índice de nombre `idx_cliente_id` en la tabla `pedidos(cliente_id)`.',
      initialCode: `-- CREATE INDEX\n`,
      solutionCode: `CREATE INDEX idx_cliente_id ON pedidos(cliente_id);`,
      testCases: [{ id: 'tc22', description: 'Crea el índice', expectedOutput: 'CREATE INDEX idx_cliente_id ON pedidos(cliente_id);' }],
      hint: 'CREATE INDEX idx_cliente_id ON pedidos(cliente_id);'
    },
    {
      id: 'sql-23',
      number: 23,
      title: 'Transacciones ACID (BEGIN, COMMIT, ROLLBACK)',
      level: 'Avanzado',
      summary: 'Garantía de atomicidad e integridad en modificaciones compuestas.',
      theoryMarkdown: `### Transacciones\n\n\`\`\`sql\nBEGIN TRANSACTION;\nUPDATE cuentas SET saldo = saldo - 100 WHERE id = 1;\nUPDATE cuentas SET saldo = saldo + 100 WHERE id = 2;\nCOMMIT;\n\`\`\``,
      instructions: 'Inicia una transacción `BEGIN;`, haz `COMMIT;` e imprime la secuencia.',
      initialCode: `-- Transacción simple\n`,
      solutionCode: `BEGIN; COMMIT;`,
      testCases: [{ id: 'tc23', description: 'Secuencia BEGIN y COMMIT', expectedOutput: 'BEGIN; COMMIT;' }],
      hint: 'BEGIN; COMMIT;'
    },
    {
      id: 'sql-24',
      number: 24,
      title: 'Expresiones Condicionales CASE WHEN',
      level: 'Avanzado',
      summary: 'Lógica condicional if-else dentro de consultas SQL.',
      theoryMarkdown: `### CASE WHEN\n\n\`\`\`sql\nSELECT nombre, \n  CASE WHEN precio > 100 THEN 'Caro' ELSE 'Barato' END AS tipo\nFROM productos;\n\`\`\``,
      instructions: 'Usa `CASE WHEN precio > 50 THEN "Alto" ELSE "Bajo" END` sobre la tabla `productos`.',
      initialCode: `-- CASE WHEN en SELECT\n`,
      solutionCode: `SELECT nombre, CASE WHEN precio > 50 THEN 'Alto' ELSE 'Bajo' END AS categoria FROM productos;`,
      testCases: [{ id: 'tc24', description: 'CASE WHEN sobre precio', expectedOutput: "SELECT nombre, CASE WHEN precio > 50 THEN 'Alto' ELSE 'Bajo' END AS categoria FROM productos;" }],
      hint: "CASE WHEN precio > 50 THEN 'Alto' ELSE 'Bajo' END"
    },
    {
      id: 'sql-25',
      number: 25,
      title: 'Funciones de Ventana: ROW_NUMBER()',
      level: 'Avanzado',
      summary: 'Enumeración de filas dentro de particiones de datos.',
      theoryMarkdown: `### ROW_NUMBER()\n\n\`\`\`sql\nSELECT nombre, ROW_NUMBER() OVER(ORDER BY precio DESC) AS ranking FROM productos;\n\`\`\``,
      instructions: 'Asigna un ranking de productos por `precio DESC` con `ROW_NUMBER() OVER(ORDER BY precio DESC)`.',
      initialCode: `-- ROW_NUMBER OVER\n`,
      solutionCode: `SELECT nombre, ROW_NUMBER() OVER(ORDER BY precio DESC) AS ranking FROM productos;`,
      testCases: [{ id: 'tc25', description: 'Ranking con ROW_NUMBER', expectedOutput: 'SELECT nombre, ROW_NUMBER() OVER(ORDER BY precio DESC) AS ranking FROM productos;' }],
      hint: 'ROW_NUMBER() OVER(ORDER BY precio DESC)'
    },
    {
      id: 'sql-26',
      number: 26,
      title: 'Funciones de Ventana: OVER(PARTITION BY)',
      level: 'Avanzado',
      summary: 'Cálculos agregados sobre subgrupos sin colapsar filas.',
      theoryMarkdown: `### PARTITION BY\n\n\`\`\`sql\nSELECT categoria, precio, AVG(precio) OVER(PARTITION BY categoria) FROM productos;\n\`\`\``,
      instructions: 'Calcula el promedio de precio particionado por `categoria`.',
      initialCode: `-- AVG OVER PARTITION BY\n`,
      solutionCode: `SELECT categoria, precio, AVG(precio) OVER(PARTITION BY categoria) AS avg_cat FROM productos;`,
      testCases: [{ id: 'tc26', description: 'AVG con PARTITION BY', expectedOutput: 'SELECT categoria, precio, AVG(precio) OVER(PARTITION BY categoria) AS avg_cat FROM productos;' }],
      hint: 'AVG(precio) OVER(PARTITION BY categoria)'
    },
    {
      id: 'sql-27',
      number: 27,
      title: 'Common Table Expressions (WITH cte AS)',
      level: 'Avanzado',
      summary: 'Tablas temporales con nombre para simplificar consultas complejas.',
      theoryMarkdown: `### CTEs\n\n\`\`\`sql\nWITH VentasAltas AS (\n  SELECT * FROM ventas WHERE monto > 1000\n)\nSELECT * FROM VentasAltas;\n\`\`\``,
      instructions: 'Crea una CTE `WITH PreciosCaros AS (SELECT * FROM productos WHERE precio > 100) SELECT * FROM PreciosCaros;`.',
      initialCode: `-- CTE con WITH\n`,
      solutionCode: `WITH PreciosCaros AS (SELECT * FROM productos WHERE precio > 100) SELECT * FROM PreciosCaros;`,
      testCases: [{ id: 'tc27', description: 'Crea la CTE con WITH', expectedOutput: 'WITH PreciosCaros AS (SELECT * FROM productos WHERE precio > 100) SELECT * FROM PreciosCaros;' }],
      hint: 'WITH PreciosCaros AS (...) SELECT * FROM PreciosCaros;'
    },
    {
      id: 'sql-28',
      number: 28,
      title: 'Procedimientos Almacenados y Triggers',
      level: 'Avanzado',
      summary: 'Ejecución de scripts automatizados ante eventos INSERT/UPDATE.',
      theoryMarkdown: `### Triggers\n\nEjecución automática de código SQL ante mutaciones en las tablas.`,
      instructions: 'Escribe una sentencia de simulación de TRIGGER o procedimiento.',
      initialCode: `-- Trigger de auditoria\n`,
      solutionCode: `CREATE TRIGGER trg_audit AFTER INSERT ON usuarios FOR EACH ROW EXECUTE FUNCTION log_audit();`,
      testCases: [{ id: 'tc28', description: 'Define TRIGGER', expectedOutput: 'CREATE TRIGGER trg_audit AFTER INSERT ON usuarios FOR EACH ROW EXECUTE FUNCTION log_audit();' }],
      hint: 'CREATE TRIGGER trg_audit AFTER INSERT ON usuarios'
    },
    {
      id: 'sql-29',
      number: 29,
      title: 'Particionamiento de Tablas Grandes',
      level: 'Avanzado',
      summary: 'División de tablas masivas por rangos de fechas o valores.',
      theoryMarkdown: `### Table Partitioning\n\nMejora dramática de rendimiento eliminando escaneos innecesarios.`,
      instructions: 'Simula la creación de una tabla particionada `BY RANGE (fecha)`.',
      initialCode: `-- PARTITION BY RANGE\n`,
      solutionCode: `CREATE TABLE ventas (id INT, fecha DATE) PARTITION BY RANGE (fecha);`,
      testCases: [{ id: 'tc29', description: 'Particionamiento por rango', expectedOutput: 'CREATE TABLE ventas (id INT, fecha DATE) PARTITION BY RANGE (fecha);' }],
      hint: 'PARTITION BY RANGE (fecha)'
    },
    {
      id: 'sql-30',
      number: 30,
      title: 'Optimización de Planes de Ejecución (EXPLAIN ANALYZE)',
      level: 'Avanzado',
      summary: 'Inspección de costo de CPU y escaneos de índices en consultas.',
      theoryMarkdown: `### EXPLAIN ANALYZE\n\n\`\`\`sql\nEXPLAIN ANALYZE SELECT * FROM productos WHERE precio > 50;\n\`\`\``,
      instructions: 'Escribe `EXPLAIN ANALYZE SELECT * FROM usuarios;`.',
      initialCode: `-- EXPLAIN ANALYZE\n`,
      solutionCode: `EXPLAIN ANALYZE SELECT * FROM usuarios;`,
      testCases: [{ id: 'tc30', description: 'Analiza el plan de ejecucion', expectedOutput: 'EXPLAIN ANALYZE SELECT * FROM usuarios;' }],
      hint: 'EXPLAIN ANALYZE SELECT * FROM usuarios;'
    }
  ]
};
