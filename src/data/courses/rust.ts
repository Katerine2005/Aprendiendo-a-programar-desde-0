import { Course } from '../../types';

export const rustCourse: Course = {
  id: 'rust',
  title: 'Rust para Sistemas',
  subtitle: 'Ownership, Borrowing, Concurrencia Segura y Macros',
  icon: 'Shield',
  color: 'from-orange-600 to-amber-700',
  badgeBg: 'bg-amber-600/10 text-amber-400 border-amber-600/20',
  description: 'Garantías del compilador sin Recolector de Basura (Garbage Collector). Domina Ownership, Borrowing, Smart Pointers, Concurrencia y Macros.',
  lessons: [
    // --- NIVEL BÁSICO (1-10) ---
    {
      id: 'rust-1',
      number: 1,
      title: 'Hola Mundo con la Macro println!',
      level: 'Básico',
      summary: 'Sintaxis básica de Rust, función main y macros con admiración !.',
      theoryMarkdown: `### Introducción a Rust\n\nRust garantiza seguridad de memoria y cero costo de abstracciones.\n\n\`\`\`rust\nfn main() {\n    println!("¡Hola, Rust!");\n}\n\`\`\``,
      instructions: 'Modifica el programa para que imprima exactamente "¡Hola Rust 2024!" con un salto de línea.',
      initialCode: `fn main() {\n    // Escribe tu código aquí\n}`,
      solutionCode: `fn main() {\n    println!("¡Hola Rust 2024!");\n}`,
      testCases: [{ id: 'tc1', description: 'Imprime ¡Hola Rust 2024!', expectedOutput: '¡Hola Rust 2024!' }],
      hint: 'println!("¡Hola Rust 2024!");'
    },
    {
      id: 'rust-2',
      number: 2,
      title: 'Variables y Mutabilidad (let mut)',
      level: 'Básico',
      summary: 'En Rust las variables son inmutables por defecto. Se usa mut para cambiarlas.',
      theoryMarkdown: `### Mutabilidad en Rust\n\n\`\`\`rust\nlet mut x = 5;\nx = 10; // Permitido gracias a 'mut'\n\`\`\``,
      instructions: 'Declara `let mut edad = 20;`, cámbiala a `25` e imprímela con `println!("{}", edad);`.',
      initialCode: `fn main() {\n    // Declara edad mut e imprímela\n}`,
      solutionCode: `fn main() {\n    let mut edad = 20;\n    edad = 25;\n    println!("{}", edad);\n}`,
      testCases: [{ id: 'tc2', description: 'Imprime 25', expectedOutput: '25' }],
      hint: 'let mut edad = 20; edad = 25; println!("{}", edad);'
    },
    {
      id: 'rust-3',
      number: 3,
      title: 'Control de Flujo con Expresiones if',
      level: 'Básico',
      summary: 'En Rust, if es una expresión que puede retornar un valor.',
      theoryMarkdown: `### if como Expresión\n\n\`\`\`rust\nlet condicion = true;\nlet numero = if condicion { 5 } else { 6 };\n\`\`\``,
      instructions: 'Usa una expresión `if` para asignar a `res` la cadena `"Si"` si `true` e imprímela.',
      initialCode: `fn main() {\n    // Exp de asignación condicional\n}`,
      solutionCode: `fn main() {\n    let res = if true { "Si" } else { "No" };\n    println!("{}", res);\n}`,
      testCases: [{ id: 'tc3', description: 'Imprime Si', expectedOutput: 'Si' }],
      hint: 'let res = if true { "Si" } else { "No" };'
    },
    {
      id: 'rust-4',
      number: 4,
      title: 'Bucles loop, while y for',
      level: 'Básico',
      summary: 'Repetición continua con loop y retorno de valores en break.',
      theoryMarkdown: `### Bucles en Rust\n\n\`\`\`rust\nfor i in 1..4 {\n    println!("{}", i);\n}\n\`\`\``,
      instructions: 'Usa a `for i in 1..4` para imprimir 1, 2 y 3 en líneas separadas.',
      initialCode: `fn main() {\n    // Bucle for 1..4\n}`,
      solutionCode: `fn main() {\n    for i in 1..4 {\n        println!("{}", i);\n    }\n}`,
      testCases: [{ id: 'tc4', description: 'Imprime 1, 2, 3', expectedOutput: "1\n2\n3" }],
      hint: 'for i in 1..4 { println!("{}", i); }'
    },
    {
      id: 'rust-5',
      number: 5,
      title: 'El Sistema de Propiedad (Ownership)',
      level: 'Básico',
      summary: 'El concepto estrella de Rust: cada valor tiene un único propietario (owner).',
      theoryMarkdown: `### Reglas de Ownership\n\n1. Cada valor en Rust tiene un propietario.\n2. Solo puede haber un propietario a la vez.\n3. Cuando el propietario sale del ámbito, el valor se libera.`,
      instructions: 'Crea una cadena `let s1 = String::from("Rust");` muévela a `s2` e imprime `s2`.',
      initialCode: `fn main() {\n    // Ownership move\n}`,
      solutionCode: `fn main() {\n    let s1 = String::from("Rust");\n    let s2 = s1;\n    println!("{}", s2);\n}`,
      testCases: [{ id: 'tc5', description: 'Imprime Rust', expectedOutput: 'Rust' }],
      hint: 'let s2 = s1; println!("{}", s2);'
    },
    {
      id: 'rust-6',
      number: 6,
      title: 'Referencias y Préstamos (Borrowing &)',
      level: 'Básico',
      summary: 'Préstamo inmutable de valores sin transferir la propiedad.',
      theoryMarkdown: `### Borrowing in Rust\n\n\`\`\`rust\nfn calcular_longitud(s: &String) -> usize {\n    s.len()\n}\n\`\`\``,
      instructions: 'Pasa una referencia `&s` a la función `medir` e imprime la longitud.',
      initialCode: `fn medir(s: &String) -> usize {\n    s.len()\n}\nfn main() {\n    let s = String::from("Hola");\n    // Llama medir(&s) e imprime\n}`,
      solutionCode: `fn medir(s: &String) -> usize {\n    s.len()\n}\nfn main() {\n    let s = String::from("Hola");\n    println!("{}", medir(&s));\n}`,
      testCases: [{ id: 'tc6', description: 'Imprime 4', expectedOutput: '4' }],
      hint: 'println!("{}", medir(&s));'
    },
    {
      id: 'rust-7',
      number: 7,
      title: 'Referencias Mutables (&mut)',
      level: 'Básico',
      summary: 'Modificación prestada con la regla de 1 sola referencia mutable.',
      theoryMarkdown: `### &mut\n\nSolo puede existir UNA referencia mutable a la vez para evitar Data Races.`,
      instructions: 'Define `modificar(s: &mut String)` que haga `s.push_str("!");`. Pasa `&mut s` e imprime `s`.',
      initialCode: `fn modificar(s: &mut String) {\n    s.push_str("!");\n}\nfn main() {\n    let mut s = String::from("Rust");\n    // Llama e imprime\n}`,
      solutionCode: `fn modificar(s: &mut String) {\n    s.push_str("!");\n}\nfn main() {\n    let mut s = String::from("Rust");\n    modificar(&mut s);\n    println!("{}", s);\n}`,
      testCases: [{ id: 'tc7', description: 'Imprime Rust!', expectedOutput: 'Rust!' }],
      hint: 'modificar(&mut s); println!("{}", s);'
    },
    {
      id: 'rust-8',
      number: 8,
      title: 'El Tipo Slice (&str y Slices de Arrays)',
      level: 'Básico',
      summary: 'Referencias a secuencias contiguas de elementos.',
      theoryMarkdown: `### Slices\n\n\`\`\`rust\nlet s = String::from("hola mundo");\nlet hola: &str = &s[0..4];\n\`\`\``,
      instructions: 'Dada `s = String::from("RustLang")`, obtén el slice `&s[0..4]` e imprímelo.',
      initialCode: `fn main() {\n    let s = String::from("RustLang");\n    // Slice 0..4 e imprime\n}`,
      solutionCode: `fn main() {\n    let s = String::from("RustLang");\n    let slice = &s[0..4];\n    println!("{}", slice);\n}`,
      testCases: [{ id: 'tc8', description: 'Imprime Rust', expectedOutput: 'Rust' }],
      hint: 'println!("{}", &s[0..4]);'
    },
    {
      id: 'rust-9',
      number: 9,
      title: 'Structs Estructurados',
      level: 'Básico',
      summary: 'Creación de tipos de datos personalizados.',
      theoryMarkdown: `### Structs\n\n\`\`\`rust\nstruct Usuario {\n    nombre: String,\n    activo: bool,\n}\n\`\`\``,
      instructions: 'Crea un struct `Punto { x: i32, y: i32 }`, instáncialo con `x:10, y:20` e imprime `p.x + p.y`.',
      initialCode: `struct Punto {\n    x: i32,\n    y: i32,\n}\nfn main() {\n    // Instancia e imprime la suma\n}`,
      solutionCode: `struct Punto {\n    x: i32,\n    y: i32,\n}\nfn main() {\n    let p = Punto { x: 10, y: 20 };\n    println!("{}", p.x + p.y);\n}`,
      testCases: [{ id: 'tc9', description: 'Imprime 30', expectedOutput: '30' }],
      hint: 'println!("{}", p.x + p.y);'
    },
    {
      id: 'rust-10',
      number: 10,
      title: 'Métodos e Impl Blocks (impl Struct)',
      level: 'Básico',
      summary: 'Asociación de funciones y métodos a estructuras con &self.',
      theoryMarkdown: `### Bloques impl\n\n\`\`\`rust\nimpl Rectangulo {\n    fn area(&self) -> u32 {\n        self.ancho * self.alto\n    }\n}\n\`\`\``,
      instructions: 'Implementa el método `area(&self) -> i32` en `Rectangulo { ancho: 5, alto: 4 }` e imprime `r.area()`.',
      initialCode: `struct Rectangulo { ancho: i32, alto: i32 }\n// impl Rectangulo\nfn main() {\n    // Instancia e imprime r.area()\n}`,
      solutionCode: `struct Rectangulo { ancho: i32, alto: i32 }\nimpl Rectangulo {\n    fn area(&self) -> i32 {\n        self.ancho * self.alto\n    }\n}\nfn main() {\n    let r = Rectangulo { ancho: 5, alto: 4 };\n    println!("{}", r.area());\n}`,
      testCases: [{ id: 'tc10', description: 'Imprime 20', expectedOutput: '20' }],
      hint: 'r.area()'
    },

    // --- NIVEL INTERMEDIO (11-20) ---
    {
      id: 'rust-11',
      number: 11,
      title: 'Enums y Pattern Matching (match)',
      level: 'Intermedio',
      summary: 'Control de flujo exhaustivo sobre tipos enumerados.',
      theoryMarkdown: `### Enums y match\n\n\`\`\`rust\nenum Estado {\n    Activo,\n    Inactivo,\n}\n\`\`\``,
      instructions: 'Evalúa un `match` sobre `Estado::Activo` e imprime `"OK"`.',
      initialCode: `enum Estado { Activo, Inactivo }\nfn main() {\n    let e = Estado::Activo;\n    // Match e e imprime OK\n}`,
      solutionCode: `enum Estado { Activo, Inactivo }\nfn main() {\n    let e = Estado::Activo;\n    match e {\n        Estado::Activo => println!("OK"),\n        Estado::Inactivo => println!("NO"),\n    }\n}`,
      testCases: [{ id: 'tc11', description: 'Imprime OK', expectedOutput: 'OK' }],
      hint: 'Estado::Activo => println!("OK")'
    },
    {
      id: 'rust-12',
      number: 12,
      title: 'El Enum Option<T> sin null',
      level: 'Intermedio',
      summary: 'Representación segura de ausencia de valor con Some(T) y None.',
      theoryMarkdown: `### Option<T>\n\nRust no tiene null. Usa \`Some(val)\` o \`None\`.`,
      instructions: 'Dada `val = Some(42)`, usa `.unwrap_or(0)` e imprímelo.',
      initialCode: `fn main() {\n    let val: Option<i32> = Some(42);\n    // unwrap_or(0) e imprime\n}`,
      solutionCode: `fn main() {\n    let val: Option<i32> = Some(42);\n    println!("{}", val.unwrap_or(0));\n}`,
      testCases: [{ id: 'tc12', description: 'Imprime 42', expectedOutput: '42' }],
      hint: 'println!("{}", val.unwrap_or(0));'
    },
    {
      id: 'rust-13',
      number: 13,
      title: 'El Enum Result<T, E> y la Macro panic!',
      level: 'Intermedio',
      summary: 'Manejo recuperable de errores con Ok(T) y Err(E).',
      theoryMarkdown: `### Result<T, E>\n\n\`\`\`rust\nfn dividir(a: f64, b: f64) -> Result<f64, String> {\n    if b == 0.0 { Err("Div/0".to_string()) } else { Ok(a / b) }\n}\n\`\`\``,
      instructions: 'Llama a `dividir(10.0, 2.0)` e imprime el valor desenvuelto con `.unwrap()`.',
      initialCode: `fn dividir(a: f64, b: f64) -> Result<f64, &'static str> {\n    if b == 0.0 { Err("Div/0") } else { Ok(a / b) }\n}\nfn main() {\n    // Llama e imprime unwrap()\n}`,
      solutionCode: `fn dividir(a: f64, b: f64) -> Result<f64, &'static str> {\n    if b == 0.0 { Err("Div/0") } else { Ok(a / b) }\n}\nfn main() {\n    println!("{}", dividir(10.0, 2.0).unwrap());\n}`,
      testCases: [{ id: 'tc13', description: 'Imprime 5', expectedOutput: '5' }],
      hint: 'dividir(10.0, 2.0).unwrap()'
    },
    {
      id: 'rust-14',
      number: 14,
      title: 'El Operador ? para Propagación de Errores',
      level: 'Intermedio',
      summary: 'Retorno temprano y conciso de errores Result.',
      theoryMarkdown: `### Operador ?\n\n\`\`\`rust\nfn procesar() -> Result<i32, &'static str> {\n    let val = dividir(10.0, 2.0)?;\n    Ok(val as i32)\n}\n\`\`\``,
      instructions: 'Usa `?` dentro de una función auxiliar que retorne `Result<i32, &str>` e imprime el resultado.',
      initialCode: `fn paso1() -> Result<i32, &'static str> { Ok(100) }\nfn paso2() -> Result<i32, &'static str> {\n    let n = paso1()?;\n    Ok(n + 50)\n}\nfn main() {\n    println!("{}", paso2().unwrap());\n}`,
      solutionCode: `fn paso1() -> Result<i32, &'static str> { Ok(100) }\nfn paso2() -> Result<i32, &'static str> {\n    let n = paso1()?;\n    Ok(n + 50)\n}\nfn main() {\n    println!("{}", paso2().unwrap());\n}`,
      testCases: [{ id: 'tc14', description: 'Imprime 150', expectedOutput: '150' }],
      hint: 'paso2().unwrap()'
    },
    {
      id: 'rust-15',
      number: 15,
      title: 'Vectores (Vec<T>) y Colecciones',
      level: 'Intermedio',
      summary: 'Arreglos dinámicos asignados en el Heap.',
      theoryMarkdown: `### Vec<T>\n\n\`\`\`rust\nlet mut v = vec![1, 2, 3];\nv.push(4);\n\`\`\``,
      instructions: 'Crea `let mut v = vec![10, 20];`, agrega `30` con `.push(30)` e imprime `v.len()`.',
      initialCode: `fn main() {\n    // Crea vector e imprime longitud\n}`,
      solutionCode: `fn main() {\n    let mut v = vec![10, 20];\n    v.push(30);\n    println!("{}", v.len());\n}`,
      testCases: [{ id: 'tc15', description: 'Imprime 3', expectedOutput: '3' }],
      hint: 'v.push(30); println!("{}", v.len());'
    },
    {
      id: 'rust-16',
      number: 16,
      title: 'HashMap<K, V> para Claves y Valores',
      level: 'Intermedio',
      summary: 'Tabla hash eficiente de la biblioteca estándar.',
      theoryMarkdown: `### HashMap\n\n\`\`\`rust\nuse std::collections::HashMap;\nlet mut map = HashMap::new();\nmap.insert("A", 1);\n\`\`\``,
      instructions: 'Inserta `"Rust" -> 2024` en un `HashMap` e imprime `map.get("Rust").unwrap()`.',
      initialCode: `use std::collections::HashMap;\nfn main() {\n    // Usa HashMap e imprime valor\n}`,
      solutionCode: `use std::collections::HashMap;\nfn main() {\n    let mut map = HashMap::new();\n    map.insert("Rust", 2024);\n    println!("{}", map.get("Rust").unwrap());\n}`,
      testCases: [{ id: 'tc16', description: 'Imprime 2024', expectedOutput: '2024' }],
      hint: 'map.get("Rust").unwrap()'
    },
    {
      id: 'rust-17',
      number: 17,
      title: 'Sintaxis if let para Desestructuración Concisa',
      level: 'Intermedio',
      summary: 'Alternativa rápida a match cuando solo interesa un único patrón.',
      theoryMarkdown: `### if let\n\n\`\`\`rust\nif let Some(x) = valor_option {\n    println!("{}", x);\n}\n\`\`\``,
      instructions: 'Dada `val = Some("Exito")`, usa `if let Some(s) = val` para imprimir `s`.',
      initialCode: `fn main() {\n    let val = Some("Exito");\n    // Usa if let\n}`,
      solutionCode: `fn main() {\n    let val = Some("Exito");\n    if let Some(s) = val {\n        println!("{}", s);\n    }\n}`,
      testCases: [{ id: 'tc17', description: 'Imprime Exito', expectedOutput: 'Exito' }],
      hint: 'if let Some(s) = val { println!("{}", s); }'
    },
    {
      id: 'rust-18',
      number: 18,
      title: 'Módulos y Visibilidad (pub)',
      level: 'Intermedio',
      summary: 'Organización jerárquica del código con mod y la palabra pub.',
      theoryMarkdown: `### Módulos\n\n\`\`\`rust\nmod mi_modulo {\n    pub fn saludar() { println!("Hola"); }\n}\n\`\`\``,
      instructions: 'Llama a `mi_modulo::saludar()`.',
      initialCode: `mod mi_modulo {\n    pub fn saludar() { println!("Modulo"); }\n}\nfn main() {\n    // Llama a la función pública del módulo\n}`,
      solutionCode: `mod mi_modulo {\n    pub fn saludar() { println!("Modulo"); }\n}\nfn main() {\n    mi_modulo::saludar();\n}`,
      testCases: [{ id: 'tc18', description: 'Imprime Modulo', expectedOutput: 'Modulo' }],
      hint: 'mi_modulo::saludar();'
    },
    {
      id: 'rust-19',
      number: 19,
      title: 'Iteradores y Adaptadores (.map, .collect)',
      level: 'Intermedio',
      summary: 'Procesamiento funcional perezoso sobre secuencias.',
      theoryMarkdown: `### Iteradores\n\n\`\`\`rust\nlet v = vec![1, 2, 3];\nlet dobles: Vec<i32> = v.iter().map(|x| x * 2).collect();\n\`\`\``,
      instructions: 'Suma los elementos de `vec![1, 2, 3]` usando `.iter().sum::<i32>()` e imprime la suma.',
      initialCode: `fn main() {\n    let v = vec![1, 2, 3];\n    // Suma con iterador e imprime\n}`,
      solutionCode: `fn main() {\n    let v = vec![1, 2, 3];\n    let suma: i32 = v.iter().sum();\n    println!("{}", suma);\n}`,
      testCases: [{ id: 'tc19', description: 'Imprime 6', expectedOutput: '6' }],
      hint: 'let suma: i32 = v.iter().sum();'
    },
    {
      id: 'rust-20',
      number: 20,
      title: 'Closures (Funciones Anónimas |x|)',
      level: 'Intermedio',
      summary: 'Captura de variables del entorno circundante.',
      theoryMarkdown: `### Closures\n\n\`\`\`rust\nlet sumar_uno = |x| x + 1;\nprintln!("{}", sumar_uno(5)); // 6\n\`\`\``,
      instructions: 'Define un closure `let doble = |x: i32| x * 2;` e imprime `doble(21)`.',
      initialCode: `fn main() {\n    // Define closure doble e imprime\n}`,
      solutionCode: `fn main() {\n    let doble = |x: i32| x * 2;\n    println!("{}", doble(21));\n}`,
      testCases: [{ id: 'tc20', description: 'Imprime 42', expectedOutput: '42' }],
      hint: 'let doble = |x: i32| x * 2;'
    },

    // --- NIVEL AVANZADO (21-30) ---
    {
      id: 'rust-21',
      number: 21,
      title: 'Traits: Interfaces en Rust',
      level: 'Avanzado',
      summary: 'Definición de comportamiento compartido entre tipos.',
      theoryMarkdown: `### Traits\n\n\`\`\`rust\ntrait Hablar {\n    fn hablar(&self) -> String;\n}\n\`\`\``,
      instructions: 'Implementa el trait `Hablar` para `struct Gato` devolviendo `"Miau"` e imprime `g.hablar()`.',
      initialCode: `trait Hablar {\n    fn hablar(&self) -> String;\n}\nstruct Gato;\n// impl Hablar for Gato\nfn main() {\n    // Llama e imprime\n}`,
      solutionCode: `trait Hablar {\n    fn hablar(&self) -> String;\n}\nstruct Gato;\nimpl Hablar for Gato {\n    fn hablar(&self) -> String { "Miau".to_string() }\n}\nfn main() {\n    let g = Gato;\n    println!("{}", g.hablar());\n}`,
      testCases: [{ id: 'tc21', description: 'Imprime Miau', expectedOutput: 'Miau' }],
      hint: 'impl Hablar for Gato { fn hablar(&self) -> String { "Miau".to_string() } }'
    },
    {
      id: 'rust-22',
      number: 22,
      title: 'Trait Bounds y Generics <T: Trait>',
      level: 'Avanzado',
      summary: 'Restricción de tipos genéricos que implementen traits específicos.',
      theoryMarkdown: `### Trait Bounds\n\n\`\`\`rust\nfn imprimir<T: std::fmt::Display>(item: T) {\n    println!("{}", item);\n}\n\`\`\``,
      instructions: 'Crea `fn mostrar<T: std::fmt::Display>(val: T)` que imprima `val`. Llama `mostrar("OK")`.',
      initialCode: `// Define mostrar con Trait Bound\nfn main() {\n    // Llama mostrar("OK")\n}`,
      solutionCode: `fn mostrar<T: std::fmt::Display>(val: T) {\n    println!("{}", val);\n}\nfn main() {\n    mostrar("OK");\n}`,
      testCases: [{ id: 'tc22', description: 'Imprime OK', expectedOutput: 'OK' }],
      hint: 'mostrar("OK");'
    },
    {
      id: 'rust-23',
      number: 23,
      title: 'Smart Pointer: Box<T> para Asignación en Heap',
      level: 'Avanzado',
      summary: 'Almacenamiento explícito en el Heap con puntero único de propiedad.',
      theoryMarkdown: `### Box<T>\n\n\`\`\`rust\nlet b = Box::new(5);\nprintln!("{}", b);\n\`\`\``,
      instructions: 'Crea un `Box::new(100)` e imprime `*b`.',
      initialCode: `fn main() {\n    // Box::new e imprime\n}`,
      solutionCode: `fn main() {\n    let b = Box::new(100);\n    println!("{}", *b);\n}`,
      testCases: [{ id: 'tc23', description: 'Imprime 100', expectedOutput: '100' }],
      hint: 'let b = Box::new(100); println!("{}", *b);'
    },
    {
      id: 'rust-24',
      number: 24,
      title: 'Smart Pointers: Rc<T> para Propiedad Múltiple',
      level: 'Avanzado',
      summary: 'Conteo de referencias inmutables en un solo hilo.',
      theoryMarkdown: `### Rc<T>\n\n\`\`\`rust\nuse std::rc::Rc;\nlet a = Rc::new(5);\nlet b = Rc::clone(&a);\nprintln!("{}", Rc::strong_count(&a)); // 2\n\`\`\``,
      instructions: 'Crea `a = Rc::new(10)`, luego `b = Rc::clone(&a)` e imprime `Rc::strong_count(&a)`.',
      initialCode: `use std::rc::Rc;\nfn main() {\n    // Rc e imprime strong_count\n}`,
      solutionCode: `use std::rc::Rc;\nfn main() {\n    let a = Rc::new(10);\n    let b = Rc::clone(&a);\n    println!("{}", Rc::strong_count(&a));\n}`,
      testCases: [{ id: 'tc24', description: 'Imprime 2', expectedOutput: '2' }],
      hint: 'Rc::strong_count(&a)'
    },
    {
      id: 'rust-25',
      number: 25,
      title: 'Arc<T> y Mutex<T> para Concurrencia Segura',
      level: 'Avanzado',
      summary: 'Punteros de conteo atómico y exclusión mutua multihilo.',
      theoryMarkdown: `### Arc y Mutex\n\nPermite compartir datos mutables entre hilos de forma segura.`,
      instructions: 'Crea `let counter = Arc::new(Mutex::new(0));` incrementa el valor protegido a `1` e imprímelo.',
      initialCode: `use std::sync::{Arc, Mutex};\nfn main() {\n    let counter = Arc::new(Mutex::new(0));\n    // Incrementa e imprime\n}`,
      solutionCode: `use std::sync::{Arc, Mutex};\nfn main() {\n    let counter = Arc::new(Mutex::new(0));\n    let mut num = counter.lock().unwrap();\n    *num += 1;\n    println!("{}", *num);\n}`,
      testCases: [{ id: 'tc25', description: 'Imprime 1', expectedOutput: '1' }],
      hint: '*num += 1; println!("{}", *num);'
    },
    {
      id: 'rust-26',
      number: 26,
      title: 'Concurrencia con std::thread y move',
      level: 'Avanzado',
      summary: 'Transferencia explicita de propiedad a hilos con la palabra move.',
      theoryMarkdown: `### threads y move\n\n\`\`\`rust\nlet v = vec![1, 2];\nlet handle = std::thread::spawn(move || {\n    println!("{:?}", v);\n});\nhandle.join().unwrap();\n\`\`\``,
      instructions: 'Lanza un hilo con `std::thread::spawn(move || { println!("Thread"); })` y espera con `.join().unwrap()`.',
      initialCode: `fn main() {\n    // Hilo spawn y join\n}`,
      solutionCode: `fn main() {\n    let handle = std::thread::spawn(move || {\n        println!("Thread");\n    });\n    handle.join().unwrap();\n}`,
      testCases: [{ id: 'tc26', description: 'Imprime Thread', expectedOutput: 'Thread' }],
      hint: 'handle.join().unwrap();'
    },
    {
      id: 'rust-27',
      number: 27,
      title: 'Canales MPSC (Multi-Producer, Single-Consumer)',
      level: 'Avanzado',
      summary: 'Paso de mensajes entre hilos con std::sync::mpsc.',
      theoryMarkdown: `### mpsc::channel\n\n\`\`\`rust\nuse std::sync::mpsc;\nlet (tx, rx) = mpsc::channel();\ntx.send("MSG").unwrap();\nprintln!("{}", rx.recv().unwrap());\n\`\`\``,
      instructions: 'Envía `"PING"` por el canal `tx` y recíbelo con `rx.recv().unwrap()` e imprímelo.',
      initialCode: `use std::sync::mpsc;\nfn main() {\n    let (tx, rx) = mpsc::channel();\n    // send y recv\n}`,
      solutionCode: `use std::sync::mpsc;\nfn main() {\n    let (tx, rx) = mpsc::channel();\n    tx.send("PING").unwrap();\n    println!("{}", rx.recv().unwrap());\n}`,
      testCases: [{ id: 'tc27', description: 'Imprime PING', expectedOutput: 'PING' }],
      hint: 'tx.send("PING").unwrap(); println!("{}", rx.recv().unwrap());'
    },
    {
      id: 'rust-28',
      number: 28,
      title: 'Anotaciones de Vida (Lifetimes \'a)',
      level: 'Avanzado',
      summary: 'Garantía del compilador de que las referencias sigan siendo válidas.',
      theoryMarkdown: `### Lifetimes \'a\n\n\`\`\`rust\nfn mas_largo<'a>(x: &'a str, y: &'a str) -> &'a str {\n    if x.len() > y.len() { x } else { y }\n}\n\`\`\``,
      instructions: 'Llama `mas_largo("largo", "corto")` e imprime el resultado.',
      initialCode: `fn mas_largo<'a>(x: &'a str, y: &'a str) -> &'a str {\n    if x.len() > y.len() { x } else { y }\n}\nfn main() {\n    // Llama e imprime\n}`,
      solutionCode: `fn mas_largo<'a>(x: &'a str, y: &'a str) -> &'a str {\n    if x.len() > y.len() { x } else { y }\n}\nfn main() {\n    println!("{}", mas_largo("largo", "corto"));\n}`,
      testCases: [{ id: 'tc28', description: 'Imprime largo', expectedOutput: 'largo' }],
      hint: 'println!("{}", mas_largo("largo", "corto"));'
    },
    {
      id: 'rust-29',
      number: 29,
      title: 'Macros Declarativas (macro_rules!)',
      level: 'Avanzado',
      summary: 'Metaprogramación para generar código en compilación.',
      theoryMarkdown: `### macro_rules!\n\n\`\`\`rust\nmacro_rules! decir {\n    ($msg:expr) => {\n        println!("{}", $msg);\n    };\n}\n\`\`\``,
      instructions: 'Invoca la macro `decir!("MacroOK");`.',
      initialCode: `macro_rules! decir {\n    ($msg:expr) => {\n        println!("{}", $msg);\n    };\n}\nfn main() {\n    // Llama a la macro\n}`,
      solutionCode: `macro_rules! decir {\n    ($msg:expr) => {\n        println!("{}", $msg);\n    };\n}\nfn main() {\n    decir!("MacroOK");\n}`,
      testCases: [{ id: 'tc29', description: 'Imprime MacroOK', expectedOutput: 'MacroOK' }],
      hint: 'decir!("MacroOK");'
    },
    {
      id: 'rust-30',
      number: 30,
      title: 'Bloques unsafe y Punteros Crudos (*const, *mut)',
      level: 'Avanzado',
      summary: 'Anulación de comprobaciones del compilador para interacción con hardware/C.',
      theoryMarkdown: `### unsafe Rust\n\nDesdereferenciar punteros crudos requiere un bloque \`unsafe\`.`,
      instructions: 'Dentro de un bloque `unsafe`, desreferencia `let num = 42; let ptr = &num as *const i32;` e imprímelo.',
      initialCode: `fn main() {\n    let num = 42;\n    let ptr = &num as *const i32;\n    // Bloque unsafe\n}`,
      solutionCode: `fn main() {\n    let num = 42;\n    let ptr = &num as *const i32;\n    unsafe {\n        println!("{}", *ptr);\n    }\n}`,
      testCases: [{ id: 'tc30', description: 'Imprime 42 desreferenciando ptr crudo', expectedOutput: '42' }],
      hint: 'unsafe { println!("{}", *ptr); }'
    }
  ]
};
